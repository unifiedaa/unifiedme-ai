# Unified proxy (core)
fastapi>=0.136.0
uvicorn>=0.45.0
httpx[socks]>=0.28.0
pydantic>=2.12.0
pydantic-settings>=2.0.0,<3.0.0
aiosqlite>=0.22.0
python-dotenv>=1.2.0
aiohttp>=3.13.0

# Gumloop (WebSocket chat + Turnstile captcha)
websockets>=12.0
2captcha-python>=2.0.0

# MCP Server (FastMCP + tools)
fastmcp>=3.2.0
Pillow>=10.0.0

# VPS Management (SSH terminal + remote orchestration)
asyncssh>=2.18.0

# Browser automation (batch login / account registration)
camoufox[geoip]>=0.4.11
playwright>=1.58.0
browserforge>=1.2.0

# CodeBuddy keygen (HTTP-based OAuth + API key creation)
curl_cffi>=0.15.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
